# Sakhi — production dependencies
# Using minimum version pins (>=) to avoid metadata-generation conflicts on Render

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Async HTTP client (Grok API + Telegram API)
httpx>=0.27.0

# MongoDB async driver — motor 3.x requires pymongo 4.x
motor>=3.3.0
pymongo>=4.6.0

# Settings / validation — pydantic v2 + matching settings
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Voice-to-text (Whisper)
# openai-whisper requires ffmpeg to be installed on the system (see render.yaml)
openai-whisper>=20231117
ffmpeg-python>=0.2.0
